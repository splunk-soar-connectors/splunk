**Unreleased**

* Regenerated the dependency lockfile to align package metadata with Splunk 4.0.2.
* Replaced the unsupported `escapejs` filter with Jinja-compatible JSON escaping in the run query widget.
